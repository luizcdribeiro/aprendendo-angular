var nome = 'Luiz Carlos'

function minhaFunc(x, y) {
  return x + y
}

let numero = 27
const pi = 3.64

var numeros = [1, 2, 3]
numeros.map((item) => item * 2)

class Matematica {
  soma(x, y) {
    return x + y
  }
}